# runtime scratch (not part of the repo)
datasets/
outputs/
logs/
# HF upload staging (weights + model card); pushed to HF, not git
hf_upload/
# python envs / caches (submodules build their own .venv)
**/.venv*/
**/__pycache__/
*.pyc
.cache/

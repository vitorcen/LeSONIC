{
  "_doc": "GR00T-SONIC demo flows. A flow is an ordered list of segments played as one looping live session (modulo loop, no GUI/server restart). Each segment: motion (one of dance|lunge|macarena|kick|squat|jump|walk), steps (control steps @50Hz, ~0.02s each), and optional full_bootstrap (replay the whole segment from the open-loop dump token instead of handing to live GR00T after the bootstrap window — used to make a one-shot locomotion clip play continuously, e.g. a walk-through between actions). Self-sustaining motions (squat/lunge/dance/macarena) run pure live; one-shot motions (kick/walk/jump) get a bootstrap window by default, or full_bootstrap for continuous playback.",

  "flow1": {
    "desc": "Discrete actions, equal time, looping. All self-sustaining (pure live GR00T, no bootstrap).",
    "segments": [
      {"motion": "squat",    "steps": 200},
      {"motion": "lunge",    "steps": 200},
      {"motion": "macarena", "steps": 200}
    ]
  },

  "flow2": {
    "desc": "All 6 deploy actions, each preceded by a walk-through, looping: walk -> kick -> walk -> jump -> walk -> dance -> walk -> macarena -> walk -> squat -> walk -> lunge (-> loops back to walk). Order updated 2026-06-15. HONEST LIVE (no full_bootstrap GT replay, removed 2026-06-15 per user — no self-deception): every segment shows the live StarVLA model. Self-sustaining actions (dance/macarena/squat/lunge) run pure live from the start. One-shot actions (walk/kick/jump) can't self-start from rest, so they get the default short ~80-step entry bootstrap to ENTER the move, then hand to live — NOT a whole-segment replay. EXPECTED: with this from-scratch head (macro-MSE 0.0236, ~19x worse than GR00T N1.7) the live span will drift/collapse and the WBC will visibly fight it — that is the honest quality of the model.",
    "segments": [
      {"motion": "walk",     "steps": 455},
      {"motion": "kick",     "steps": 165},
      {"motion": "walk",     "steps": 455},
      {"motion": "jump",     "steps": 500},
      {"motion": "walk",     "steps": 455},
      {"motion": "dance",    "steps": 497},
      {"motion": "walk",     "steps": 455},
      {"motion": "macarena", "steps": 1375},
      {"motion": "walk",     "steps": 455},
      {"motion": "squat",    "steps": 424},
      {"motion": "walk",     "steps": 455},
      {"motion": "lunge",    "steps": 399}
    ]
  },

  "flow3": {
    "desc": "flow3 LAFAN hand-picked windows, user order fight1->run3->fight2->run2->dance1->run1, looping. LIVE GR00T (outputs/gr00t_sonic_flow3): each window starts mid-action (one-shot) so it gets a short bootstrap to ENTER the move, then hands to live GR00T inference. One-shot windows may settle toward standing after entry (the known no-memory limitation); the offline gear_sonic_flow3.sh loop is the guaranteed-smooth version. Add full_bootstrap:true to a segment to replay it whole (continuous, but not live). Requires GR00T_CKPT=outputs/gr00t_sonic_flow3/checkpoint-6000 PKL=data/seg_flow3_all.pkl PRED_DIR=datasets/sonic_vla_pred_flow3.",
    "segments": [
      {"motion": "block",    "steps": 542},
      {"motion": "circle",   "steps": 749},
      {"motion": "combat",   "steps": 749},
      {"motion": "sprint",   "steps": 749},
      {"motion": "moonwalk", "steps": 999},
      {"motion": "jogback",  "steps": 749}
    ]
  }
}
